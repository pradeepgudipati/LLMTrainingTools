services:
  llm-training-tools:
    build: .
    ports:
      - "5000:5000"
    environment:
      FLASK_RUN_HOST: 0.0.0.0
      FLASK_RUN_PORT: 5000
      FLASK_SECRET_KEY: change-me-before-sharing
    volumes:
      - ./src/data:/app/src/data
